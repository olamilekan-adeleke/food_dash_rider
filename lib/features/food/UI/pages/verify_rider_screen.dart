import 'package:flutter/material.dart';

class VerifyRiderScreen extends StatelessWidget {
  const VerifyRiderScreen();
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('VerifyRiderScreen'),
      ),
      body: Center(
        child: Container(
          child: Text('VerifyRiderScreen'),
        ),
      ),
    );
  }
}
